/**
 * E2E tests are scoped to a sibling task (`05-02-integration-and-quality-verification`).
 * This file is a placeholder so the directory exists.
 */
import { describe, it } from 'vitest';

describe.skip('e2e (out of scope for this task)', () => {
  it('placeholder', () => {
    /* See 05-02-integration-and-quality-verification. */
  });
});
